[profile.default]
src = 'src'
out = 'out'
libs = ['lib']
fs_permissions = [{ access = "read-write", path = "./"}]
evm_version = 'cancun'
optimizer = true
optimizer_runs = 200
via_ir = false

gas_reports = ["*"]
